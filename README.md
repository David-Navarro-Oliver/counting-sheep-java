# Counting Sheep - Java

## 📌 Enunciado

Recibirás una lista de valores booleanos:

- `true` → oveja
- `false` → lobo

La aplicación debe:

- Contar cuántas ovejas hay.
- Si no quedan ovejas, mostrar que los lobos se las han comido.
- No se puede usar `else`.

---

## 🧠 Lógica implementada

- Se utiliza un método `public static` en `SheepCounter`.
- Se cuentan los valores `true`.
- Si el total es mayor que 0 → se muestra el número de ovejas.
- Si el total es 0 → se muestra el mensaje de alerta.

---

## ▶ Cómo ejecutar

Ejecutar la clase `Main`.

---

## ✅ Salida esperada

```
There are 17 sheep in total
UPS!!! Wolfs eaten Sheeps
```